use crate::randomizer::{Graph, Result};

pub fn standard_shuffle<N, E>(graph: &mut impl Graph<N, E>) -> Result<()> {
    Ok(())
}

pub fn chaos_shuffle<N, E>(graph: &mut impl Graph<N, E>) -> Result<()> {
    Ok(())
}
